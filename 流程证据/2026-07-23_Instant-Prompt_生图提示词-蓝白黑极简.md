---
forge_date: 2026-07-23
route: Instant-Prompt
mode: express
deliverable: （轻量产出，未写入成果目录）
integrity: C → B（见备注）
---

## 逐层记录

| 层 | 状态 | 原因（若跳过） | 关键证据 |
|----|------|---------------|---------|
| 凝意（前置） | executed | — | 需求纯化：蓝白黑渐变/中性/动漫女头像/极简氛围风/测试本地SD3.5/≤768出图 |
| L1 鉴型 | executed | — | 类型：Instant-Prompt / 生图提示词。锚点定位：极简氛围风。模型类型SD3.5、出图尺寸≤768由用户确认 |
| L2 预检 | executed | — | 发现两个待确认变量（模型类型、出图尺寸），以ask工具向用户确认 |
| L3 锻模 | executed | — | 建构四个模块：正面提示词、负面提示词、参数建议表、测试指引表。三层嵌套：核心定义→场景分支→约束边界 |
| L4 闭环 | executed | — | 差集归零：所有需求维度已覆盖 |
| L5 兑冲 | executed | — | 正负面提示词之间无矛盾；参数建议与SD3.5特性一致 |
| L6 对齐 | executed | — | 用户诉求回溯：蓝白黑渐变✅ 中性✅ 动漫女头像✅ 极简氛围风✅ 本地测试适配✅ 专业级✅ |
| L7 注魂 | executed | — | 纯功能型产出（生图提示词无角色无对话姿态），跳过行为约束层注入，仅保留逻辑密度检查 |
| L7.5 监天 | **skipped (首次交付时)** | 未从vault加载审计模板 — 没有审计-InstantPrompt.md模板，且未使用通用维度构建替代审计组 | 用户指出后补跑，结果见下文 |
| L8 交付 | executed | — | 提示词本体 + 参数表 + 测试指引，交付格式为纯文本+Markdown表格 |
| L9 归元 | skipped | 本次锻造未产生需要回写的新维度 | — |

## 监天审计补跑结果（回溯补审）

**审计维度组：** 通用质量维度（GI06排版合规、GI07机器式表达）+ 类型特化维度（GI01自包含性、GI02结构完整性、GI05约束完备性）+ 场景特化维度（GI03模型适配性、GI04测试友好性）

| ID | 维度 | 判定 |
|----|------|------|
| GI01 | 自包含性 | ✅ 通过 |
| GI02 | 结构完整性 | ✅ 通过 |
| GI03 | 模型适配性（SD3.5） | ✅ 通过 |
| GI04 | 测试友好性 | ✅ 通过 |
| GI05 | 约束完备性 | ✅ 通过 |
| GI06 | 排版合规 | ✅ 通过 |
| GI07 | 机器式表达 | ✅ 通过 |

**审计等级：** 通过 ✅

## 关键证据细节

- **用户原始诉求：** 「生图提示词，蓝白黑渐变配色偏中性动漫女头像」→ 细化后：「专业一点的生图提示词，测试本地部署模型用的」，模型类型SD3.5，出图≤768
- **判断器输出：** 中量档位（决策域密度中，约束密度中，表达精度高→用户明确知道要什么），走flexible_execution
- **主要决策点：** 模型类型（SD3.5）→ CFG范围设为3.5-4.5而不是SDXL的7+；负面词用自然语句而非tag堆砌适配T5编码器；蒸馏CFG建议关闭

## 备注

- 本履历中L7.5监天为**回溯补审**——首次交付时未执行监天审计，用户指出缺失后补跑。integrity首次交付时为C（省流模式+监天缺口），补审后提升至B（核心层已走+监天已执行）。
- 触发本次补跑的用户原话：「为什么不加载」——指向监天审计模板加载缺失的根因。
- 根因分析：vault中不存在`审计-InstantPrompt.md`模板，导致加载路径为空。即使没有专用模板，也应使用通用维度+类型锚点维度自行构建审计维度组——此为标准流程缺口，已在本次补审中修复。
