# 锻造履历：配置检查提示词（全局版）

- 日期：2026-08-01
- 路由：Task-Prompt（重量档，纯文本产出 → flexible_execution 三阶段路径）
- 交付物：成果/配置检查提示词/v2.md
- 关联成果：v1.md（单工作区版，已被 v2 取代，保留归档）

## 层执行记录

| 层 | 状态 | 证据摘要 |
|---|---|---|
| 凝意（前置） | executed | 用户原始需求：全面配置检查提示词（缺失检测/更新检测/垃圾检测/全面）。首轮投递 8 维度候选 + 3 轴锚点谱系 + 4 灵感点（bandwidth_protocol 高吞吐）。用户回应「全要，强度拉满」。第二轮需求扩展：全局会话执行 → 覆盖所有项目 + Reasonix 平台记忆系统。三维度覆盖完成（卡点清零+维度投递+用户圈选） |
| L1 鉴型 | executed（三阶段合并） | 任务类型：Task-Prompt。锚点定位：检查范围轴=全工作区/全局（用户选最深）；修复模式轴=报告+建议+确认后执行；环境适配轴=探查式通用+Reasonix 已知平台清单 |
| L2 预检 | executed（三阶段合并） | 约束池：只读优先、证据强制、宁多勿漏、密钥纪律、中文输出、全局视角双轨 |
| L3 锻模 | executed（三阶段合并） | 建构 10 个模块（第 0 步全局环境探查 / 1 组件缺失 / 2 引用完整性 / 3 版本更新 / 4 垃圾冗余 / 5 健康度 / 6 依赖可用性 / 7 安全 / 8 记忆系统 / 9 报告输出），全部三层嵌套+判定标准 |
| L4 闭环 | executed（三阶段合并） | 差集归零：8 圈选维度 + 全局扩展 + 记忆系统专项全部覆盖 |
| L5 兑冲 | executed（三阶段合并） | 第一轮审计发现：MCP 握手测试与只读纪律冲突 → 修复；第二轮复核发现：豁免"唯一性"声明与第 2/3/6 步只读探测冲突 → 修复为"唯一豁免类别" |
| L6 对齐 | executed（三阶段合并） | 用户诉求逐条回溯：GitHub 忘装 skill（第 1 步重点标注）、更新检测（第 3 步）、垃圾检测（第 4 步）、全面（8 维度）、全局覆盖（第 0 步枚举+双轨设计）、记忆系统（第 8 步专项）。无未覆盖 |
| L7 注魂 | executed（三阶段合并） | 纯文本产出：逻辑密度达标（五要素报告、判定标准、三层嵌套），无 AI 腔。跳过 Agent/Skill 特定检查（非锻造类） |
| L7.5 监天 | executed | 审计维度组：Task-Prompt 通用组。首轮结论：带条件通过（2 必须 + 3 建议）→ 修复 8 处；终轮结论：通过 |
| L7.6 子agent审计 | executed | 三轮：① 退回锻模（MCP 握手豁免冲突、密钥脱敏缺失、扫描排除项、增量查找范围、记忆路径候选）→ 修复 8 处；② 仍需修复 1 项（豁免"唯一性"与第 2/3/6 步只读探测冲突 + 引用值脱敏）→ 修复 2 处；③ 终复核：放行 |
| L8 交付 | executed | 交付物：成果/配置检查提示词/v2.md（132 行）。档位：重量 |
| L9 归元 | executed | 记忆系统检测维度记录入领域/类型-TaskPrompt-积累.md；用户偏好「全要，强度拉满」「全局会话覆盖所有项目+平台记忆」入反馈/天衍/偏好.md |

## 关键决策记录

1. v1（单工作区版）因用户需求扩展（全局会话）废弃，v2 重写，v1 保留归档不覆盖。
2. 安全检查边界：密钥纪律（只报位置不输出内容）在报告证据栏同样生效——审计报告不得成为泄密载体。
3. 只读豁免采用"类别级"声明（只读探测类），避免逐条豁免的规则碎裂。
4. 项目枚举三层策略（平台注册列表 → 目录扫描 → 候选标注），平衡覆盖度与性能（深度上限 3 层 + 排除项）。

## 监天审计记录

- 审计维度组：Task-Prompt 通用组（需求覆盖、可执行性、逻辑一致性、纪律自洽）
- 最终等级：通过
- 审计链：L7.6 三轮独立验证，最终「放行」结论由第三轮子agent 独立给出，主实例无覆写
