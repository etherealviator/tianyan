---
forge_date: 2026-08-05
route: Knowledge-Artifact + 自我迭代
mode: forge
deliverable: 领域/审计-框架声明一致性.md + .reasonix/attachments/tianyan-prompt.xml
integrity: A
---

# 框架声明一致性审计模板 · 交付 + v0.30 引用落地

> 用户指令：「把这个审计提示词也整一份吧，省的每次都现想」「单独为刚刚的行为做一份提示词，然后以后的天衍按着这个来就行」——把 v0.29 的"新机制联动审计"行为固化为可复用审计模板，并接入框架标准流程。

## 逐层记录

| 层 | 状态 | 原因（若跳过） | 关键证据 |
|----|------|---------------|---------|
| 凝意（前置） | executed | — | 用户两次澄清：①要一份审计提示词模板（非 skill 安装）②单独为"新机制引入后声明一致性审计"这个行为做提示词，以后天衍按此执行 |
| L1 鉴型 | executed | — | 双路由交付：Knowledge-Artifact（模板本体，领域/审计-框架声明一致性.md）+ 天衍自我迭代（框架引用，v0.30） |
| L2 预检 | executed | — | 约束池：模板须从 v0.21/v0.29 两轮实战提炼（三区划分/十查维度/历史锚点）；框架引用须最小化（constraint 5 一句+self_check 一项）；引用路径必须真实存在 |
| L3 锻模 | executed | — | 模板本体：三区划分（A 路由矩阵/B 锻造引擎/C 审计交付侧+vault）、十查审计维度（路由声明/审计分级/履历格式/核验协议/豁免冲突/归元路径/降级路径/数字名称残留/措辞张力/无缺口确认）、输出格式（必须更新/建议同步/无缺口确认三分）、修复闭环（全修→validate→备份两步分开→履历→git）、历史锚点表（10 个真实案例）、使用说明（转子agent prompt 自包含+fleet 并行+模板随框架演进） |
| L4 闭环 | executed | — | 模板 6 节 + 框架引用 2 处全部消耗归零 |
| L5 兑冲 | executed | — | 模板×审计模板家族（领域/审计-*.md）：本模板管"声明一致性"，审计-{类型}.md 管"交付物质量"——分工声明清楚；模板×constraint 5：联动审计是收尾动作不是新审计层，不与 L7.5/L7.6 冲突；引用路径已核验存在 |
| L6 对齐 | executed | — | 用户诉求回溯：模板固化✓ 框架引用✓（"以后的天衍按着这个来"=self_check+联动审计规则） |
| L7 注魂 | executed | — | 模板命令式可执行，无 AI 腔；历史锚点均为真实案例 |
| L7.5 监天 | executed | — | validate-prompt.js：1619 标签配对零错误、checklist 连续、残留全清；版本 v0.30 |
| L7.6 淬炼 | executed | — | 本轮自我迭代按新模板自查（涉及新声明）：十查全部通过——引用路径真实、无路由/履历/核验协议需同步、无豁免冲突、无残留 |
| L8 交付 | executed | — | 模板写入 领域/审计-框架声明一致性.md；v0.30 落盘；备份 v0.30-20260805.xml |
| L9 归元 | executed | — | 审计模板家族积累（领域/审计-积累.md 未改——本模板独立新增）；优化清单追加 v0.30 段 |

## 关键证据细节

- 用户原始诉求：「把这个审计提示词也整一份吧，省的每次都现想」「不不不我的意思就是单独为刚刚的行为做一份提示词，然后以后的天衍按着这个来就行」
- 模板内容提炼自：v0.21（全模块审计：38 处修复）+ v0.29（新机制联动审计：17 处修复）两轮实战——三区划分、十查维度、历史锚点表（10 个真实案例：路由遗漏机制/术语漂移/归元断裂/豁免冲突/履历滞后/核验缺口/数字残留/降级未声明/裁决主体措辞/审查落点写死）
- 框架引用落点：forge_mandate constraint 5 新增「联动审计收尾」规则 + self_check 补审计项
- 校验脚本：node scripts/validate-prompt.js —— 1619 标签（+2）配对零错误、checklist 1-12 连续、版本 v0.30
- 主要决策点：模板放 领域/ 目录（审计模板家族成员，与审计-{类型}.md 并列，职责分工声明）；框架引用最小化（不新增审计层，只加收尾规则）
- 监天审计结论：通过

## 备注

- 模板本身是活的：新机制引入后若暴露本模板未覆盖的声明类型，审计后回写模板（历史锚点追加）——模板随框架演进，使用说明中已声明
- 已知缺陷 #29（审计模板 GSP06）仍 open，模板侧
- 终检②剩余措辞级建议（b-1/b-2/c-1/c-2）仍挂起
